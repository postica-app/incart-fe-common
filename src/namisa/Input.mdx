# Input

import Input from "./Input"
import { iconList } from "../icons"

<Namisa
    controls={{
        showIcon: {
            default: false,
        },
        icon: {
            options: iconList,
            default: iconList[0],
        },
        placeholder: {
            default: "Placeholder",
        },
    }}
>
    {({ args }) => <Input args={args} />}
</Namisa>
