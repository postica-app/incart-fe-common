# Input

import Input from './Input'
import { iconList } from '../icons'

<Namisa
    controls={{
        showIcon: {
            default: true,
        },
        icon: {
            options: iconList,
            default: iconList[0],
        },
        placeholder: {
            default: 'Placeholder',
        },
        disabled: {
            default: true,
        },
    }}
>
    {({ args }) => <Input {...args} />}
</Namisa>
