# Recheck Button

중요한 일을 할 때 두 번 체크하도록 하는 버튼

import RecheckButton from './RecheckButton.tsx'

<Namisa
    controls={{
        active: {
            default: false,
        },
    }}
>
    {({ args }) => <RecheckButton {...args} />}
</Namisa>
