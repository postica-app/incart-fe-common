import { Hexile } from "@haechi/flexile"
import { styled } from "../../stitches.config"

export const styles = {
    Container: styled(Hexile, {
        backgroundColor: "$grey1",
        borderRadius: "3rem",
    }),
}
